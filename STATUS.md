# Status

## works today
- workspace compiles
- in-memory history append/read tests
- `cascade version` and `cascade start`

## still sketched
- durable backends, real workers, compensations

CI fails if cargo test fails.
